# Tracks Generator

A web-based 3D configurator for designing Lego Duplo track layouts. Built with React and React Three Fiber, the app allows users to drag and snap track pieces together. It implements LDraw Units (LDU).


## Running the project

```bash
npm run dev